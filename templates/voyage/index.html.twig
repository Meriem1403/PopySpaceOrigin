{% extends 'base.html.twig' %}

{% block title %}Liste des voyages{% endblock %}

{% block body %}
    <div class="card-popy m-4 p-4 rounded-lg">
        <div class="flex justify-between items-center mb-6">
            <h1 class="text-2xl font-bold text-popy-light">🚀 Voyages interplanétaires</h1>

            <a href="{{ path('app_voyage_new') }}" class="btn-popy inline-flex items-center space-x-2">
                <span>Créer un voyage</span>
                <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" fill="none">
                    <path stroke="none" d="M0 0h24v24H0z" />
                    <path d="M12 5v14m-7 -7h14" />
                </svg>
            </a>
        </div>

        <table class="min-w-full text-sm text-popy-white">
            <thead class="border-b border-popy-primary text-popy-light uppercase text-xs">
            <tr>
                <th class="px-6 py-3 text-left">ID</th>
                <th class="px-6 py-3 text-left">Objectif</th>
                <th class="px-6 py-3 text-left">Date de départ</th>
                <th class="px-6 py-3 text-left">Actions</th>
            </tr>
            </thead>
            <tbody class="divide-y divide-popy-primary">
            {% for voyage in voyages %}
                <tr class="hover:bg-popy-primary/30 transition">
                    <td class="px-6 py-4">{{ voyage.id }}</td>
                    <td class="px-6 py-4 font-semibold">{{ voyage.objectif }}</td>
                    <td class="px-6 py-4">{{ voyage.depart ? voyage.depart|date('d/m/Y') : '—' }}</td>
                    <td class="px-6 py-4 italic text-popy-accent text-sm">
                        à venir
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4" class="px-6 py-4 text-center text-popy-white/70 italic">
                        Aucun voyage pour le moment
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
