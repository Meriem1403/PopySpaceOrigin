<form method="post" action="{{ path('app_voyage_delete', {'id': voyage.id}) }}"
      onsubmit="return confirm('Es-tu sûr de vouloir supprimer ce voyage ?');"
>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voyage.id) }}">
    <button
        class="px-4 py-2 text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700 focus:ring-4 focus:ring-red-300 focus:outline-none"
    >
        Supprimer
    </button>
</form>
