{# templates/components/bouton.html.twig #}
{% props balise='button', classesVariante='bg-green-600 hover:bg-green-700 text-white' %}

<{{ balise }} {{ attributes.defaults({
    'class': 'px-4 py-2 border border-transparent text-sm font-medium rounded-md ' ~ classesVariante,
}) }}>
{% block content %}{% endblock %}
</{{ balise }}>
