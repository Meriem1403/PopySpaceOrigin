{% extends 'base.html.twig' %}

{% block title %}Gérer les planètes{% endblock %}

{% block body %}
    <div class="card-popy m-4 p-4 rounded-lg">
        <h1 class="text-2xl text-popy-light font-bold mb-6">🌍 Planètes connues</h1>

        <table class="min-w-full text-popy-white text-sm">
            <thead class="border-b border-popy-light uppercase text-xs text-popy-light">
            <tr>
                <th class="px-6 py-3 text-left">ID</th>
                <th class="px-6 py-3 text-left">Nom</th>
                <th class="px-6 py-3 text-left">Description</th>
                <th class="px-6 py-3 text-left">Distance (AL)</th>
                <th class="px-6 py-3 text-left">Actions</th>
            </tr>
            </thead>
            <tbody class="divide-y divide-popy-primary">
            {% for planete in planetes %}
                <tr class="hover:bg-popy-primary/30">
                    <td class="px-6 py-4">{{ planete.id }}</td>
                    <td class="px-6 py-4 font-semibold">{{ planete.nom }}</td>
                    <td class="px-6 py-4">{{ planete.description }}</td>
                    <td class="px-6 py-4">{{ planete.distanceLumiereTerre }}</td>
                    <td class="px-6 py-4 italic text-popy-accent text-sm">
                        (pas encore de lien)
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="px-6 py-4 text-center text-popy-white/70 italic">
                        Aucune planète enregistrée
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="mt-6 text-right">
            <span class="btn-popy cursor-not-allowed opacity-60">
                Créer une planète (à venir)
            </span>
        </div>
    </div>
{% endblock %}
